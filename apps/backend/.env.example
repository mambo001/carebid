PORT=8080
APP_NAME=CareBid
DATABASE_URL=postgresql://neon:neon@127.0.0.1:5432/neondb
ALLOWED_ORIGINS=http://localhost:5173
REDIS_URL=redis://127.0.0.1:6379

# Firebase Configuration
# Development: Uses Firebase Auth Emulator (configured in docker-compose.dev.yml)
# Production: Uses Application Default Credentials (ADC) with real Firebase
FIREBASE_PROJECT_ID=carebid-local
